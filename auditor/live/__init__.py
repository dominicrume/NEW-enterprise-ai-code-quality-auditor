"""Live audit surface — one command, a browser, zero configuration.

The terminal commands assume a user who already knows what a spec is and
is willing to author YAML before seeing anything. That is a wall for
everyone else. ``auditor live`` removes it: run one command, a page opens,
and metrics move as the code changes.
"""
