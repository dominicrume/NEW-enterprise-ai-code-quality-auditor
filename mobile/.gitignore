node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects are generated by `expo prebuild` — not checked in.
/ios
/android

# Credentials — never commit. Store secrets in EAS, not the repo.
*.jks
*.p8
*.p12
*.key
*.mobileprovision
google-services.json
GoogleService-Info.plist

.DS_Store
*.log
